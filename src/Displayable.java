import java.awt.*;

/**
 * Displayable is the main interface to implement the methods that draw for the different class.
 */

public interface Displayable {
    void Draw(Graphics g);
    void paintComponents(Graphics g);
}
