import java.awt.image.BufferedImage;

/**
 * TrapSprite is used for PhysicEngine to know what does a trap is.
 */

public class TrapSprite extends DynamicSprite{ //Class for the trap sprite
    public TrapSprite(BufferedImage image, double x, double y, double width, double height) {
        super(image, x, y, width, height);
    }



}
